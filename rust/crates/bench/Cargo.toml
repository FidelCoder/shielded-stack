[package]
name = "bench"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
lwd-client = { path = "../lwd-client" }
serde = { version = "1", features = ["derive"] }

[lib]
path = "src/lib.rs"
